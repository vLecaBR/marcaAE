/**
 * (Removido no Q6) O mock do financeiro da clínica foi eliminado — os dados vêm de
 * `GET /finance/teams/{teamId}/summary`. Sem movimentação → empty state neutro.
 * Arquivo mantido vazio apenas porque a exclusão física não é permitida neste ambiente.
 */
export {}
