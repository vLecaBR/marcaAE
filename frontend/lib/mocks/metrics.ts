/**
 * (Removido no Q6) O mock do dashboard financeiro foi eliminado — os dados vêm de
 * `GET /finance/summary`. Sem movimentação → empty state neutro.
 * Arquivo mantido vazio apenas porque a exclusão física não é permitida neste ambiente.
 */
export {}
