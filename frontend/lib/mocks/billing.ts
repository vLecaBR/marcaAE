/**
 * (Removido no Q6) O mock de billing (`MOCK_TEAM_BILLING`) foi eliminado — a API e a sessão ditam
 * o plano de ponta a ponta. O default neutro do plano free vive agora em `lib/api/billing.ts`.
 * Arquivo mantido vazio apenas porque a exclusão física não é permitida neste ambiente.
 */
export {}
