/**
 * (Removido no Q6) O mock de clínica (`MOCK_CLINIC`) foi eliminado — a identidade da clínica vem
 * do backend (`GET /teams` / `GET /teams/{id}`). Sem clínica → empty state "Crie sua clínica".
 * Arquivo mantido vazio apenas porque a exclusão física não é permitida neste ambiente.
 */
export {}
