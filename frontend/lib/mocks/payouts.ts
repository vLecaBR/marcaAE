/**
 * (Removido no Q6) Os mocks de recebimentos (conta/saldo/extrato) foram eliminados — a conta vem
 * de `GET /payouts`; saldo/extrato mostram empty state até o provedor expor os valores reais.
 * Arquivo mantido vazio apenas porque a exclusão física não é permitida neste ambiente.
 */
export {}
